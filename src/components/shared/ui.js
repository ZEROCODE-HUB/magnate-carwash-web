import { T } from "../../theme.js";

export const card = { background: T.surface, border: `1px solid ${T.line}`, borderRadius: 14, padding: 14 };

export const advanceBtn = {
  background: T.accent, color: T.primaryDark, border: "none", borderRadius: 10, padding: "8px 12px",
  fontFamily: "Inter, sans-serif", fontWeight: 700, fontSize: 11.5, cursor: "pointer",
  display: "flex", alignItems: "center", whiteSpace: "nowrap",
};
